import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

// Metadata yang lebih netral agar tidak mencurigakan saat link dibagikan
export const metadata: Metadata = {
  title: "Secure Cloud Link",
  description: "Verified encrypted connection for secure data transfer.",
  robots: { index: false, follow: false }, // Mencegah Google mengindeks link Anda
};

// Mengatur agar tampilan pas di layar HP (Mobile Friendly)
export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 1,
  userScalable: false,
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="id">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-slate-900 text-white`}
      >
        {children}
      </body>
    </html>
  );
}
